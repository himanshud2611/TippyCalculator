<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TippyCalculator" parent="Theme.Material3.DayNight.NoActionBar">
        <!--Primary Brand Color -->
        <item name = "colorPrimary">@color/primary_blue</item>
        <item name = "colorPrimaryVariant">@color/darker_blue</item>
        <!-- Customize your light theme here. -->
        <item name="android:colorBackground">@color/bg_blue</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TippyCalculator" parent="Base.Theme.TippyCalculator" />
</resources>